@using BlogIt.Components.Shared
@using BlogIt.Services
@inject IPublicContentService ContentService

<SeoHead Title="Installed package smoke"
         Description="Public component compilation proof"
         SchemaType="SeoSchemaType.WebPage" />
<GaScript />

@code {
    private PublicPostPage? posts;

    protected override async Task OnInitializedAsync()
    {
        posts = await ContentService.GetPostsAsync(1, 1);
    }
}
